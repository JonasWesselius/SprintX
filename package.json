{
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "drizzle-orm": "^0.38.3",
    "pg": "^8.13.1",
    "prisma": "^6.1.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.30.1"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate:pg",
    "db:migrate": "tsx src/lib/migrate.ts",
    "db:drop": "drizzle-kit drop",
    "db:check": "drizzle-kit check:pg",
    "db:up": "drizzle-kit up:pg",
    "db:studio": "drizzle-kit studio --port 3333 --verbose"
  }
}
